# AURASOUND

A static HTML, CSS, and JavaScript ecommerce demo.

Open `index.html` in a browser to run the site.

Note: browser-only HTML/JS cannot securely use private `.env` secrets. Any API key used directly by JavaScript is visible to users, so the assistant uses local product recommendations in this static version.
